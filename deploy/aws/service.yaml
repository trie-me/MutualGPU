AWSTemplateFormatVersion: '2010-09-09'
Description: MutualGPU public ALB and one ARM64 EC2-backed ECS service using clean PostgreSQL and S3 state.

Parameters:
  FoundationStackName:
    Type: String
    Default: mutualgpu-foundation
  ImageUri:
    Type: String
    AllowedPattern: '^428590861908\.dkr\.ecr\.us-east-1\.amazonaws\.com/mutualgpu-api@sha256:[a-f0-9]{64}$'
    ConstraintDescription: Must be the reviewed Quinn-MutualCompute ECR image URI pinned by immutable digest.
  CertificateArn:
    Type: String
    AllowedPattern: '^arn:aws:acm:us-east-1:428590861908:certificate/[0-9a-f-]+$'
    ConstraintDescription: Must be an ACM certificate ARN in target account 428590861908 and us-east-1.
  PublicApiHost:
    Type: String
    Default: mutualgpu.com
    AllowedPattern: '^[A-Za-z0-9][A-Za-z0-9.-]*[A-Za-z0-9]$'
    ConstraintDescription: Must be the DNS hostname covered by CertificateArn, without a scheme or path.
    Description: Public DNS hostname covered by CertificateArn and routed to this load balancer.
  DeploymentSecretArn:
    Type: String
    AllowedPattern: '^arn:aws:secretsmanager:us-east-1:428590861908:secret:mutualgpu/deployment-[A-Za-z0-9]+$'
    ConstraintDescription: Must be the target mutualgpu/deployment secret ARN without exposing its value.
  DesiredCount:
    Type: Number
    Default: 1
    AllowedValues:
      - 0
      - 1
    Description: Use 1 for the approved steady state; 0 is available for a reviewed stop-before-start operation.
  PostgresRuntimeMode:
    Type: String
    Default: 'false'
    AllowedValues:
      - 'false'
      - 'true'
    Description: Keep false while support is installed and import is verified. Switch to true only after the approved write freeze and migration gates pass.
  ActiveLegacyTaskDefinitionArn:
    Type: String
    AllowedPattern: '^arn:aws:ecs:us-east-1:428590861908:task-definition/mutualgpu-api:[0-9]+$'
    Description: Exact currently deployed legacy task definition retained while PostgresRuntimeMode is false.
  MaintenanceMode:
    Type: String
    Default: 'false'
    AllowedValues:
      - 'false'
      - 'true'
    Description: Independent ALB barrier. Set true before stopping writers or importing data; it returns 503 except for the reviewed operator /32.
  MaintenanceOperatorIpv4Cidr:
    Type: String
    Default: 192.0.2.1/32
    AllowedPattern: '^([0-9]{1,3}\.){3}[0-9]{1,3}/32$'
    Description: Exact reviewed operator IPv4 /32 allowed through the maintenance barrier. The documentation example address is intentionally unusable.
  ProviderCorsOrigin0:
    Type: String
    Default: 'https://huggingface.co'
    Description: Exact Chrome-provider origin. No wildcard matching is enabled.
  ProviderCorsOrigin1:
    Type: String
    Default: 'https://vercel.com'
    Description: Exact Chrome-provider origin. No wildcard matching is enabled.
  ProviderCorsOrigin2:
    Type: String
    Default: 'https://yosun-triposplat-webgpu-demo.static.hf.space'
    Description: Exact Chrome-provider origin. No wildcard matching is enabled.
  ProviderEnrollmentRateLimit:
    Type: Number
    Default: 10
    MinValue: 10
    Description: Maximum provider-identifier mints per source IP during one minute.
  AllowArbitraryBrowserTaskWriteOrigins:
    Type: String
    Default: 'true'
    AllowedValues:
      - 'false'
      - 'true'
    Description: Preserve the deployed permissive task-write-origin behavior; changing it requires an explicit reviewed D9 decision.

Conditions:
  HasProviderCorsOrigin0: !Not [!Equals [!Ref ProviderCorsOrigin0, '']]
  HasProviderCorsOrigin1: !Not [!Equals [!Ref ProviderCorsOrigin1, '']]
  HasProviderCorsOrigin2: !Not [!Equals [!Ref ProviderCorsOrigin2, '']]
  UsePostgresRuntime: !Equals [!Ref PostgresRuntimeMode, 'true']
  MaintenanceEnabled: !Equals [!Ref MaintenanceMode, 'true']

Resources:
  LoadBalancer:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: mutualgpu-demo
      Scheme: internet-facing
      Type: application
      IpAddressType: ipv4
      SecurityGroups:
        - Fn::ImportValue: !Sub '${FoundationStackName}-AlbSecurityGroupId'
      Subnets:
        - Fn::ImportValue: !Sub '${FoundationStackName}-PublicSubnetAId'
        - Fn::ImportValue: !Sub '${FoundationStackName}-PublicSubnetBId'
      LoadBalancerAttributes:
        - Key: idle_timeout.timeout_seconds
          Value: '600'
        - Key: client_keep_alive.seconds
          Value: '604800'
      Tags:
        - Key: Name
          Value: mutualgpu-demo
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  ProviderEnrollmentWebAcl:
    Type: AWS::WAFv2::WebACL
    Properties:
      Name: mutualgpu-provider-enrollment
      Scope: REGIONAL
      Description: Limits anonymous provider-identifier minting while leaving enrollment open to everyone.
      DefaultAction:
        Allow: {}
      CustomResponseBodies:
        enrollmentRateLimited:
          ContentType: APPLICATION_JSON
          Content: '{"code":"enrollment_rate_limited"}'
      Rules:
        - Name: provider-enrollment-rate-limit
          Priority: 0
          Action:
            Block:
              CustomResponse:
                ResponseCode: 429
                CustomResponseBodyKey: enrollmentRateLimited
          Statement:
            RateBasedStatement:
              AggregateKeyType: IP
              Limit: !Ref ProviderEnrollmentRateLimit
              EvaluationWindowSec: 60
              ScopeDownStatement:
                AndStatement:
                  Statements:
                    - ByteMatchStatement:
                        FieldToMatch:
                          Method: {}
                        PositionalConstraint: EXACTLY
                        SearchString: POST
                        TextTransformations:
                          - Priority: 0
                            Type: NONE
                    - ByteMatchStatement:
                        FieldToMatch:
                          UriPath: {}
                        PositionalConstraint: EXACTLY
                        SearchString: /api/webgpu-enrollments
                        TextTransformations:
                          - Priority: 0
                            Type: NONE
          VisibilityConfig:
            CloudWatchMetricsEnabled: true
            MetricName: providerEnrollmentRateLimit
            SampledRequestsEnabled: false
      VisibilityConfig:
        CloudWatchMetricsEnabled: true
        MetricName: mutualgpuProviderEnrollment
        SampledRequestsEnabled: false
      Tags:
        - Key: Name
          Value: mutualgpu-provider-enrollment
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  ProviderEnrollmentWebAclAssociation:
    Type: AWS::WAFv2::WebACLAssociation
    Properties:
      ResourceArn: !Ref LoadBalancer
      WebACLArn: !GetAtt ProviderEnrollmentWebAcl.Arn

  WebTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: mutualgpu-web
      TargetType: instance
      Protocol: HTTP
      Port: 8080
      VpcId:
        Fn::ImportValue: !Sub '${FoundationStackName}-VpcId'
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: /health/ready
      HealthCheckIntervalSeconds: 15
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: '200'
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: '15'
      Tags:
        - Key: Name
          Value: mutualgpu-web
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  GrpcTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: mutualgpu-grpc
      TargetType: instance
      Protocol: HTTP
      ProtocolVersion: GRPC
      Port: 8081
      VpcId:
        Fn::ImportValue: !Sub '${FoundationStackName}-VpcId'
      # Kestrel's native gRPC port is HTTP/2 only. ALB's standard gRPC health
      # method is intentionally not implemented by the application, which
      # produces the expected gRPC UNIMPLEMENTED status (12).
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: /AWS.ALB/healthcheck
      HealthCheckIntervalSeconds: 15
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        GrpcCode: '12'
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: '15'
      Tags:
        - Key: Name
          Value: mutualgpu-grpc
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  HttpListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref LoadBalancer
      Port: 80
      Protocol: HTTP
      DefaultActions:
        - Type: redirect
          RedirectConfig:
            Protocol: HTTPS
            Port: '443'
            StatusCode: HTTP_301
      Tags:
        - Key: Name
          Value: mutualgpu-http
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  HttpsListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref LoadBalancer
      Port: 443
      Protocol: HTTPS
      SslPolicy: ELBSecurityPolicy-TLS13-1-2-Res-2021-06
      Certificates:
        - CertificateArn: !Ref CertificateArn
      DefaultActions:
        - !If
          - MaintenanceEnabled
          - Type: fixed-response
            FixedResponseConfig:
              StatusCode: '503'
              ContentType: application/json
              MessageBody: '{"code":"maintenance"}'
          - Type: forward
            TargetGroupArn: !Ref WebTargetGroup
      ListenerAttributes:
        - Key: routing.http.response.content_security_policy.header_value
          Value:
            Fn::Sub:
              - >-
                default-src 'self'; script-src 'self' 'wasm-unsafe-eval' https://cdn.jsdelivr.net; style-src 'self'; style-src-attr 'unsafe-inline'; img-src 'self' blob: https://api.producthunt.com https://${BucketName}.s3.${AWS::Region}.amazonaws.com; connect-src 'self' https://cdn.jsdelivr.net https://huggingface.co https://*.hf.co https://*.xethub.hf.co; base-uri 'none'; form-action 'self'; frame-ancestors 'none'; object-src 'none'
              - BucketName:
                  Fn::ImportValue: !Sub '${FoundationStackName}-ApplicationDataBucketName'
      Tags:
        - Key: Name
          Value: mutualgpu-https
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  GrpcRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref HttpsListener
      Priority: 10
      Conditions:
        - Field: path-pattern
          PathPatternConfig:
            Values:
              - /mutualgpu.v1.ProviderControl/*
      Actions:
        - !If
          - MaintenanceEnabled
          - Type: fixed-response
            FixedResponseConfig:
              StatusCode: '503'
              ContentType: application/json
              MessageBody: '{"code":"maintenance"}'
          - Type: forward
            TargetGroupArn: !Ref GrpcTargetGroup
      Tags:
        - Key: Name
          Value: mutualgpu-grpc
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Ingress
        - Key: Lifecycle
          Value: Persistent

  MaintenanceOperatorWebRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: MaintenanceEnabled
    Properties:
      ListenerArn: !Ref HttpsListener
      Priority: 5
      Conditions:
        - Field: source-ip
          SourceIpConfig:
            Values:
              - !Ref MaintenanceOperatorIpv4Cidr
      Actions:
        - Type: forward
          TargetGroupArn: !Ref WebTargetGroup

  MaintenanceOperatorGrpcRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: MaintenanceEnabled
    Properties:
      ListenerArn: !Ref HttpsListener
      Priority: 4
      Conditions:
        - Field: source-ip
          SourceIpConfig:
            Values:
              - !Ref MaintenanceOperatorIpv4Cidr
        - Field: path-pattern
          PathPatternConfig:
            Values:
              - /mutualgpu.v1.ProviderControl/*
      Actions:
        - Type: forward
          TargetGroupArn: !Ref GrpcTargetGroup

  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: mutualgpu-api
      RequiresCompatibilities:
        - EC2
      NetworkMode: bridge
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn:
        Fn::ImportValue: !Sub '${FoundationStackName}-TaskExecutionRoleArn'
      TaskRoleArn:
        Fn::ImportValue: !Sub '${FoundationStackName}-PostgresCandidateTaskRoleArn'
      ContainerDefinitions:
        - Name: api
          Image: !Ref ImageUri
          Essential: true
          Cpu: 256
          MemoryReservation: 256
          Memory: 384
          PortMappings:
            - ContainerPort: 8080
              HostPort: 8080
              Protocol: tcp
              AppProtocol: http
            - ContainerPort: 8081
              HostPort: 8081
              Protocol: tcp
              AppProtocol: grpc
          Environment:
            - Name: ASPNETCORE_ENVIRONMENT
              Value: Production
            - Name: MutualGPU__Security__AllowArbitraryBrowserTaskWriteOrigins
              Value: !Ref AllowArbitraryBrowserTaskWriteOrigins
            - Name: MutualGPU__TrustForwardedProto
              Value: 'true'
            - Name: MutualGPU__TrustedProxyNetworks__0
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-PublicSubnetACidr'
            - Name: MutualGPU__TrustedProxyNetworks__1
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-PublicSubnetBCidr'
            - Name: MutualGPU__S3__BucketName
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-ApplicationDataBucketName'
            - Name: MutualGPU__S3__Region
              Value: us-east-1
            - Name: MutualGPU__ObjectStorage__WriteTargetId
              Value: aws-primary
            - Name: MutualGPU__ObjectStorage__SynchronizeBrowserCors
              Value: 'false'
            - Name: MutualGPU__ProviderKeyS3__BucketName
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-ApplicationDataBucketName'
            - Name: MutualGPU__ProviderKeyS3__Region
              Value: us-east-1
            - Name: MutualGPU__Csp__S3ImageOrigin
              Value:
                Fn::Sub:
                  - 'https://${BucketName}.s3.${AWS::Region}.amazonaws.com'
                  - BucketName:
                      Fn::ImportValue: !Sub '${FoundationStackName}-ApplicationDataBucketName'
            - Name: MutualGPU__Postgres__Host
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseEndpointAddress'
            - Name: MutualGPU__Postgres__Port
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseEndpointPort'
            - Name: MutualGPU__Postgres__Database
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseName'
            - Name: MutualGPU__Postgres__Username
              Value: mutualgpu
            - Name: MutualGPU__Postgres__RequireTls
              Value: 'true'
            - Name: MutualGPU__Postgres__MinimumPoolSize
              Value: '1'
            - Name: MutualGPU__Postgres__MaximumPoolSize
              Value: '5'
            - Name: MutualGPU__Postgres__OrphanArtifactReconciliation__Enabled
              Value: 'false'
            - Name: MutualGPU__Postgres__OrphanArtifactReconciliation__ReportOnly
              Value: 'true'
            - !If
              - HasProviderCorsOrigin0
              - Name: MutualGPU__ProviderCorsOrigins__0
                Value: !Ref ProviderCorsOrigin0
              - !Ref AWS::NoValue
            - !If
              - HasProviderCorsOrigin1
              - Name: MutualGPU__ProviderCorsOrigins__1
                Value: !Ref ProviderCorsOrigin1
              - !Ref AWS::NoValue
            - !If
              - HasProviderCorsOrigin2
              - Name: MutualGPU__ProviderCorsOrigins__2
                Value: !Ref ProviderCorsOrigin2
              - !Ref AWS::NoValue
          Secrets:
            - Name: MutualGPU__Admin__MasterPassword
              ValueFrom: !Sub '${DeploymentSecretArn}:adminMasterPassword::'
            - Name: MutualGPU__Postgres__Password
              ValueFrom:
                Fn::Join:
                  - ''
                  - - Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseSecretArn'
                    - ':password::'
            - Name: MutualGPU__Postgres__HandleEncryptionKey
              ValueFrom: !Sub '${DeploymentSecretArn}:taskHandleEncryptionKey::'
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: /ecs/mutualgpu-api
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: api
      Tags:
        - Key: Name
          Value: mutualgpu-api
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Compute
        - Key: Lifecycle
          Value: Persistent

  MigrationTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: mutualgpu-postgres-migration
      RequiresCompatibilities:
        - EC2
      NetworkMode: bridge
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn:
        Fn::ImportValue: !Sub '${FoundationStackName}-TaskExecutionRoleArn'
      TaskRoleArn:
        Fn::ImportValue: !Sub '${FoundationStackName}-PostgresMigrationTaskRoleArn'
      ContainerDefinitions:
        - Name: migrator
          Image: !Ref ImageUri
          Essential: true
          Cpu: 256
          MemoryReservation: 256
          Memory: 384
          EntryPoint:
            - dotnet
          Command:
            - migrator/MutualGPU.TransactionalDataMigrator.dll
            - import
          Environment:
            - Name: MUTUALGPU_TARGET_TASK_MODE
              Value: 'true'
            - Name: AWS_REGION
              Value: us-east-1
            - Name: MUTUALGPU_MIGRATION_APPLICATION_BUCKET
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-ApplicationDataBucketName'
            - Name: MUTUALGPU_MIGRATION_PROVIDER_BUCKET
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-ApplicationDataBucketName'
            - Name: MUTUALGPU_MIGRATION_WRITE_STORAGE_TARGET_ID
              Value: aws-primary
            - Name: MutualGPU__Postgres__Host
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseEndpointAddress'
            - Name: MutualGPU__Postgres__Port
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseEndpointPort'
            - Name: MutualGPU__Postgres__Database
              Value:
                Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseName'
            - Name: MutualGPU__Postgres__Username
              Value: mutualgpu
            - Name: MutualGPU__Postgres__RequireTls
              Value: 'true'
          Secrets:
            - Name: MutualGPU__Postgres__Password
              ValueFrom:
                Fn::Join:
                  - ''
                  - - Fn::ImportValue: !Sub '${FoundationStackName}-DatabaseSecretArn'
                    - ':password::'
            - Name: MutualGPU__Postgres__HandleEncryptionKey
              ValueFrom: !Sub '${DeploymentSecretArn}:taskHandleEncryptionKey::'
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: /ecs/mutualgpu-api
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: postgres-migration
      Tags:
        - Key: Name
          Value: mutualgpu-postgres-migration
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Migration
        - Key: Lifecycle
          Value: CutoverSupport

  Service:
    Type: AWS::ECS::Service
    DependsOn:
      - HttpListener
      - HttpsListener
      - GrpcRule
    Properties:
      ServiceName: mutualgpu-api
      Cluster:
        Fn::ImportValue: !Sub '${FoundationStackName}-EcsClusterArn'
      TaskDefinition: !If [UsePostgresRuntime, !Ref TaskDefinition, !Ref ActiveLegacyTaskDefinitionArn]
      DesiredCount: !Ref DesiredCount
      CapacityProviderStrategy:
        - CapacityProvider:
            Fn::ImportValue: !Sub '${FoundationStackName}-EcsCapacityProviderName'
          Base: 1
          Weight: 1
      HealthCheckGracePeriodSeconds: 120
      DeploymentConfiguration:
        MinimumHealthyPercent: 0
        MaximumPercent: 100
        DeploymentCircuitBreaker:
          Enable: true
          Rollback: true
      EnableECSManagedTags: true
      PropagateTags: SERVICE
      LoadBalancers:
        - ContainerName: api
          ContainerPort: 8080
          TargetGroupArn: !Ref WebTargetGroup
        - ContainerName: api
          ContainerPort: 8081
          TargetGroupArn: !Ref GrpcTargetGroup
      Tags:
        - Key: Name
          Value: mutualgpu-api
        - Key: Application
          Value: MutualGPU
        - Key: Project
          Value: MutualGPU
        - Key: Environment
          Value: Production
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Owner
          Value: Quinn-MutualCompute
        - Key: Component
          Value: Compute
        - Key: Lifecycle
          Value: Persistent

Outputs:
  LoadBalancerDnsName:
    Value: !GetAtt LoadBalancer.DNSName
  HttpsUrl:
    Description: Certificate-valid public API base URL.
    Value: !Sub 'https://${PublicApiHost}'
  LoadBalancerHttpsUrl:
    Description: Raw ALB HTTPS URL. Use only for infrastructure diagnostics; it is not certificate-valid unless CertificateArn covers this hostname.
    Value: !Sub 'https://${LoadBalancer.DNSName}'
  ServiceName:
    Value: !GetAtt Service.Name
  MigrationTaskDefinitionArn:
    Value: !Ref MigrationTaskDefinition
